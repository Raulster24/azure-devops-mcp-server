# Azure DevOps Configuration
# Required: Your Azure DevOps organization URL
AZDO_ORG_URL=https://dev.azure.com/yourorganization

# Required: Personal Access Token with appropriate permissions
# Scopes needed: Work Items (Read), Wiki (Read), Test Management (Read)
AZDO_PAT=your-personal-access-token-here

# Optional: Default project name to use when not specified
AZDO_DEFAULT_PROJECT=YourDefaultProject

# Optional: Maximum content length for summaries (default: 2000)
AZDO_MAX_CONTENT_LENGTH=2000

# Optional: Cache timeout in milliseconds (default: 300000 = 5 minutes)
AZDO_CACHE_TIMEOUT=300000

# Optional: HTTP request timeout in milliseconds (default: 120000 = 2 minutes)
# Increase this if you get timeout errors with large Azure DevOps projects
AZDO_HTTP_TIMEOUT=120000

# Example values (replace with your actual values):
# AZDO_ORG_URL=https://dev.azure.com/contoso
# AZDO_PAT=abcdef1234567890abcdef1234567890abcdef12
# AZDO_DEFAULT_PROJECT=ContosoProject
# AZDO_HTTP_TIMEOUT=300000  # 5 minutes for very large projects